[package]
name = "toad-token"
version = "0.1.0"
edition = "2021"

[dependencies]
charms-sdk = { path = "../../charms-sdk" }

[profile.release]
lto = "fat"
opt-level = "s"
strip = "symbols"

[workspace]
